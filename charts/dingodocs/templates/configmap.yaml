apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dingodocs.fullname" . }}-config
  labels:
    {{- include "dingodocs.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
