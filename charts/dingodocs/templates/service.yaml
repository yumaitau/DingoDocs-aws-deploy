apiVersion: v1
kind: Service
metadata:
  name: {{ include "dingodocs.fullname" . }}-web
  labels:
    {{- include "dingodocs.labels" . | nindent 4 }}
    app.kubernetes.io/component: web
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dingodocs.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: web
